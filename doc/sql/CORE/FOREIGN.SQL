ALTER TABLE ADMINS ADD
    CONSTRAINT FK_ADMIN_ID
    FOREIGN KEY(id)
    REFERENCES UTILISATEURS(id) 
;

ALTER TABLE PERSONNELS ADD
    CONSTRAINT FK_PERSONNE_ID
    FOREIGN KEY(id)
    REFERENCES UTILISATEURS(id) 
;
ALTER TABLE PERSONNELS ADD
    CONSTRAINT FK_PER_LIGUE
    FOREIGN KEY(id_ligue)
    REFERENCES LIGUES(id)
;

ALTER TABLE RESERVATIONS ADD
    CONSTRAINT FK_RESERVATION_ID
    FOREIGN KEY(id_personnel)
    REFERENCES PERSONNELS(id)
;
ALTER TABLE RESERVATIONS ADD
    CONSTRAINT FK_RESERV_STA
    FOREIGN KEY(type_statut)
    REFERENCES STATUTS(id)
;
ALTER TABLE RESERVATIONS ADD
    CONSTRAINT FK_RESERV_PARKING
    FOREIGN KEY(numero_place)
    REFERENCES PLACES(numero)
;
