ALTER TABLE ADMIN ADD
    CONSTRAINT FK_ADMIN_ID
    FOREIGN KEY(id)
    REFERENCES UTILISATEUR(id) 
    NOT NULL
;

ALTER TABLE PERSONNE ADD
    CONSTRAINT FK_PERSONNE_ID
    FOREIGN KEY(id)
    REFERENCES UTILISATEUR(id) 
    NOT NULL
;

ALTER TABLE RESERVATION ADD
    CONSTRAINT FK_RESERVATION_ID
    FOREIGN KEY(id)
    REFERENCES PERSONNEL(id)
    NOT NULL
;
ALTER TABLE RESERVATION ADD
    CONSTRAINT FK_RESERV_STA
    FOREIGN KEY(type_statut)
    REFERENCES STATUT(type_statut)
    NOT NULL
;
ALTER TABLE RESERVATION ADD
    CONSTRAINT FK_RESERV_PARKING
    FOREIGN KEY(id_place)
    REFERENCES PLACE_PARKING(id_place)
;
